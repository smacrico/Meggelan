[build]
dockerfilePath = "deploy/dashboard/Dockerfile"

[deploy]
startCommand = "streamlit run app/dashboard/streamlit_app.py --server.port=8080 --server.address=0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/"
healthcheckTimeout = 300
